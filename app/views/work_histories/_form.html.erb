<%= form_for(work_history) do |f| %>
  <% if work_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(work_history.errors.count, "error") %> prohibited this work_history from being saved:</h2>

      <ul>
      <% work_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :company_name %>
    <%= f.text_field :company_name %>
  </div>

  <div class="field">
    <%= f.label :joined_at %>
    <%= f.datetime_select :joined_at %>
  </div>

  <div class="field">
    <%= f.label :leaved_at %>
    <%= f.datetime_select :leaved_at %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
